import type { ErrorStrings } from "@arcgis/charts-shared-utils";
import { WebChartTypes } from "@arcgis/charts-spec";
/**
 * Pulls the proper placeholder according to the chart type
 * @param chartType The chart type to get the placeholder for
 * @category Private
 */
export declare function getChartPlaceholder(chartType?: WebChartTypes, strings?: ErrorStrings): string | undefined;
