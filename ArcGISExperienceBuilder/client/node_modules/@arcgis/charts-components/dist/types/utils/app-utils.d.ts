/**
 * Default timers for debouncing.
 */
export declare const DebounceTimers: {
    ModelConfigChange: 5;
};
/**
 * Takes a map from event names to handler functions, then adds listeners for each event
 * @param listeningTarget Target on which the event listeners will be added
 * @param events Map of events
 */
export declare function addEventListenersFromMap(listeningTarget: EventTarget, events: Map<string, (e: CustomEvent) => void>): void;
