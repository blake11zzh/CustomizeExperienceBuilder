import type { ChartConfig } from "@arcgis/charts-shared-utils";
/**
 * Gets the series id from the config
 */
export declare const getSeriesId: (seriesIndex: number, config: ChartConfig) => string | undefined;
/**
 * Gets the series index from the config
 */
export declare const getSeriesIndex: (seriesId: string, config: ChartConfig) => number | undefined;
/**
 * Gets the length of the series from the config
 */
export declare const getSeriesLength: (config: ChartConfig) => number;
