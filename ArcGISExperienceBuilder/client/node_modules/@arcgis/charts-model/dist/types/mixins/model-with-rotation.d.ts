import { ChartModel } from "../chart-model/chart-model";
export declare abstract class ModelWithRotation extends ChartModel {
    /**
     * Gets whether the chart is rotated.
     * @returns {boolean}
     */
    getRotatedState(): boolean;
    /**
     * Sets whether the chart is rotated.
     * @param {boolean} newRotatedState
     */
    setRotatedState(newRotatedState: boolean): void;
}
