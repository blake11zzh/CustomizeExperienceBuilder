import type { ChartConfig } from "@arcgis/charts-shared-utils";
/**
 * Gets the color match.
 *
 * @param config the chart configuration
 */
export declare const getColorMatch: (config: ChartConfig) => boolean;
/**
 * Sets the color match by updating the config accordingly.
 *
 * @param newColorMatch the new color match value
 * @param config the chart configuration
 */
export declare const setColorMatch: (newColorMatch: boolean, config: ChartConfig) => void;
