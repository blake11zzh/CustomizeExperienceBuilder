import type { ChartTypes } from "@arcgis/charts-shared-utils";
import type { SupportedLayer, WebMapWebChart } from "@arcgis/charts-spec";
import type { ModelOfType } from "../interfaces/common";
/**
 * Creates chart model to use for config interaction.
 * @param props.layer The layer to use for the model.
 * @param props.chartType The chart type to create.
 * @param props.config Optional starting config for the model.
 */
export declare function createModel<T extends ChartTypes | undefined>(props: {
    layer: SupportedLayer;
    chartType: T;
    config?: WebMapWebChart;
}): Promise<ModelOfType<T> | undefined>;
