import type { ValueAxisDataItem, ValueAxis } from "@amcharts/amcharts4/charts";
import type { WebChartAxis } from "@arcgis/charts-spec";
import type { NumberFormatter } from "@amcharts/amcharts4/core";
export interface HistogramTickProps {
    axisTick: ValueAxisDataItem;
    value: number;
    axisConfig: WebChartAxis;
    numberFormatter: NumberFormatter;
}
export interface UpdateTicksProps {
    valueAxis: ValueAxis;
    axisConfig: WebChartAxis;
    ticks?: number[];
}
