import type { XYChart } from "@amcharts/amcharts4/charts";
import type { InitializeBarAndLineChartAxesProps, UpdateBarAndLineChartProps } from "./interfaces";
/**
 * Indicates when the axes need to be reinitialized:
 * - when the dual axis options changed
 * - when the rotation changed
 * - when the axis value format changed
 * - when the time interval information was reset
 * - when the axis labels behavior changed
 * @param props.chart The chart
 * @param props.rotationChanged Indicates if the chart rotation has changed
 * @param props.dualAxisChanged Indicates if the dual axis has changed (a 3rd axis was added or removed)
 * @param props.timeIntervalInfoWasReset Indicates if the time interval information was reset
 * @param props.axisLabelsBehaviorChanged Indicates if the axis labels behavior has changed
 */
export declare function isInitializingAxesNeeded(props: {
    chart: XYChart;
    rotationChanged?: boolean;
    dualAxisChanged?: boolean;
    timeIntervalInfoWasReset?: boolean;
    axisLabelsBehaviorChanged?: [boolean, boolean];
}): boolean;
export declare function initializeBarAndLineChartAxes(props: InitializeBarAndLineChartAxesProps): void;
/**
 * Updates the axes of a bar and line chart.
 *
 * @param props.chart The chart to update
 * @param props.chartConfig The chart configuration
 * @param props.rotationChanged Indicates if the chart rotation has changed
 * @param props.dualAxisChanged Indicates if the dual axis has changed (a 3rd axis was added or removed)
 * @param props.timeIntervalInfoWasReset Whether the time interval information was reset
 * @param props.axisLabelsBehaviorChanged Indicates if the axis labels behavior has changed
 * @param props.isRotated Indicates if the chart is rotated
 * @param props.timeIntervalInfo The time interval information
 * @param props.guideTooltipFormatter The guide tooltip HTML formatter
 * @param props.xAxisLabelFormatter The X axis label formatter
 * @param props.yAxisLabelFormatter The Y axis label formatter
 * @param props.commonStrings The common t9n strings
 * @param props.errorStrings The t9n error strings
 */
export declare function updateBarAndLineChartAxes(props: UpdateBarAndLineChartProps): Promise<void>;
