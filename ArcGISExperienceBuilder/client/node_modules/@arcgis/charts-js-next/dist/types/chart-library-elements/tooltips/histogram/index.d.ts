import type { ColumnSeries } from "@amcharts/amcharts5/xy";
import type { HistogramLabelFormatCallback } from "@arcgis/charts-js-common";
import type { HistogramTooltipProps } from "./interfaces";
import type { WebChartHistogramSeries } from "@arcgis/charts-spec";
import type { Series, Sprite, Tooltip } from "@amcharts/amcharts5";
/**
 * Creates an adapter callback that uses the formatter stored in the series memory storage
 * @param props
 */
export declare function generateDefaultHistogramTooltipHTMLFormatter(props: HistogramTooltipProps): HistogramLabelFormatCallback;
/**
 * Adds the html code to the tooltip associated to the histogram
 * @param seriesConfig The series config
 * @param series the series parent of the element
 * @param tooltip the tooltip that we are adding editing
 */
export declare function histogramTooltipHTMLCallback(props: {
    seriesConfig: WebChartHistogramSeries;
    series: Series;
    tooltip: Sprite | Tooltip;
}): string | undefined;
/**
 * Initializes the tooltip associated to the columns on a histogram
 * @param series The Series containing the columns template on which to add the tooltip html text adapter
 */
export declare function initializeHistogramTooltip(series: ColumnSeries): void;
/**
 * Updates the histogram tooltip formatter, by using the custom formatter provided through the chart options
 * or generating a default one.
 * @param props
 */
export declare function updateHistogramTooltip(props: HistogramTooltipProps): void;
