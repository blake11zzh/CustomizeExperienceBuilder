import type { DataItem } from "@amcharts/amcharts5";
import { AxisRendererCircular } from "@amcharts/amcharts5/radar";
import type { Axis, AxisRenderer, IAxisDataItem } from "@amcharts/amcharts5/xy";
import { RESTSymbolType } from "@arcgis/charts-spec";
import type { WebChartGuide, WebChartTextSymbol } from "@arcgis/charts-spec";
/**
 * Initializes a guide label
 * @param props.guide The guide on which to initialize the label
 * @param props.axis The axis related to the label
 * @param props.guideLabelConfig The guide's label config
 */
export declare function initializeGuideLabel(props: {
    guide: DataItem<IAxisDataItem>;
    axis: Axis<AxisRenderer | AxisRendererCircular>;
    guideLabelConfig?: WebChartTextSymbol;
}): void;
/**
 * Updates guide label
 * @param props.axis The axis related to the label
 * @param props.guide The guide on which to update the label
 * @param props.guideConfig The guide's config
 * @param props.guideType Indicating whether the guide is a surface or a line
 */
export declare function updateGuideLabel(props: {
    axis: Axis<AxisRenderer | AxisRendererCircular>;
    guide: DataItem<IAxisDataItem>;
    guideConfig: WebChartGuide;
    guideType: RESTSymbolType;
}): void;
