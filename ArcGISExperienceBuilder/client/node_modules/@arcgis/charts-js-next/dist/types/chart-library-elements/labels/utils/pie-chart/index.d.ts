import type { PieChart } from "@amcharts/amcharts5/percent";
import type { PieChartLabelFormatCallback } from "@arcgis/charts-js-common";
/**
 * Generic callback used by the pie chart for its:
 *   - data labels
 *   - tooltips
 *   - legend markers
 * When dataTooltipVisible is present and set to false, then an empty string will be returned; otherwise:
 *    If formats a slice's value and percentage according to the format options from the config, or uses
 *    a default formatter.
 *    The values are displayed as:
 *      - if only value is requested: 'value'
 *      - if only percentage is requested: 'percentage%'
 *      - if value and percentage are requested: 'percentage% (value)'
 * @param props.pieChart The pie chart
 * @param props.valueIntlOptions The value format options
 * @param props.percentageIntlOptions The percentage format options
 * @param props.displayNumericValue Whether to display the numeric value on the label
 * @param props.displayPercentage Whether to display the percentage on the label
 * @param props.valuePrefix The prefix to add to the value
 * @param props.valueSuffix The suffix to add to the value
 * @param props.percentagePrefix The prefix to add to the percentage
 * @param props.percentageSuffix The suffix to add to the percentage
 */
export declare function generatePieChartValueFormatter(props: {
    pieChart: PieChart;
    valueIntlOptions?: Intl.NumberFormatOptions;
    percentageIntlOptions?: Intl.NumberFormatOptions;
    displayNumericValue?: boolean;
    displayPercentage?: boolean;
    valuePrefix?: string;
    valueSuffix?: string;
    percentagePrefix?: string;
    percentageSuffix?: string;
}): PieChartLabelFormatCallback;
