import type { Graphics, Sprite, Template, Tooltip } from "@amcharts/amcharts5";
import type { LineSeries } from "@amcharts/amcharts5/xy";
import type { ScatterPlotTooltipProps } from "./interfaces";
import type { WebChartScatterPlotSeries } from "@arcgis/charts-spec";
/**
 * Adds the html code to the tooltip associated to the scatter plot bullets
 * @param seriesConfig The series config
 * @param markerSeries the line series parent of the element
 * @param tooltip the tooltip that we are adding editing
 */
export declare function scatterPlotTooltipHTMLCallback(props: {
    seriesConfig: WebChartScatterPlotSeries;
    markerSeries: LineSeries;
    tooltip: Sprite | Tooltip;
}): string | undefined;
/**
 * Initializes the tooltip associated to the markers on a scatter plot
 * @param element The bullet element which has the tooltip on it
 * @param markerSeries The series on which to initialize the tooltip
 */
export declare function initializeScatterPlotTooltip(element: Template<Graphics>, markerSeries: LineSeries): void;
/**
 * Updates/initializes the tooltip for a Sprite marker in Scatter Plot.
 * @param props.axesConfig The axes config
 * @param props.seriesConfig The series config
 * @param props.series The series on which to update the tooltip
 * @param props.tooltipFormatter The tooltip formatter to use
 */
export declare function updateScatterPlotTooltip(props: ScatterPlotTooltipProps): void;
