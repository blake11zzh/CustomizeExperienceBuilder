import type { SerialChart } from "@amcharts/amcharts5";
import type { ChartOptions } from "@arcgis/charts-js-common";
import type { CommonStrings, ErrorStrings, PieSlicesSymbols, QueryObject } from "@arcgis/charts-shared-utils";
import type { WebChart, WebChartDataTypes, WebGaugeChart } from "@arcgis/charts-spec";
/**
 * Initializes the chart series and sets default properties.
 * @param props.chart The chart
 * @param props.chartConfig The chart configuration
 * @param props.options The runtime options
 * @param props.commonStrings The common t9n strings
 */
export declare function initializeSeries(props: {
    chart: SerialChart;
    chartConfig: WebChart;
    options?: ChartOptions;
    commonStrings: CommonStrings;
}): Promise<void>;
/**
 * Update the chart series
 * @param props.chart The chart
 * @param props.chartConfig The chart configuration
 * @param props.seriesIndexMapper The map object containing the series index for the series id
 * @param props.options The runtime options
 * @param props.data The chart data to pass to the series
 * @param props.dataWasUpdated Whether the data was updated
 * @param props.sortChanged Whether the chart sort changed
 * @param props.seriesWereReinitialized Whether the chart series were reinitialized before an update
 * @param props.rotationChanged Whether the chart rotation changed
 * @param props.timeZoneChanged Whether the time zone changed
 * @param props.pieSlicesSymbols The pre-computed pie slices symbols
 * @param props.queryObject The query object
 * @param props.commonStrings The common t9n strings
 * @param props.errorStrings The t9n error strings
 */
export declare function updateSeries(props: {
    chart: SerialChart;
    chartConfig: WebChart | WebGaugeChart;
    seriesIndexMapper?: Map<string, number>;
    options?: ChartOptions;
    data?: WebChartDataTypes;
    dataWasUpdated?: boolean;
    sortChanged?: boolean;
    seriesWereReinitialized?: boolean;
    rotationChanged?: boolean;
    timeZoneChanged?: boolean;
    pieSlicesSymbols?: PieSlicesSymbols;
    queryObject?: QueryObject;
    commonStrings: CommonStrings;
    errorStrings: ErrorStrings;
}): Promise<void>;
