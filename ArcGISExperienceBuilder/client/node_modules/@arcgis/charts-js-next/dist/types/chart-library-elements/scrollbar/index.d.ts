import type { Chart } from "@amcharts/amcharts5";
import type { WebChartAxisScrollBar } from "@arcgis/charts-spec";
/**
 * Initializes the scrollbars for the chart
 * @param chart The chart
 */
export declare function initializeChartScrollbars(chart: Chart): void;
/**
 * Updates scrollbars on the chart
 * @param props.chart The chart
 * @param props.scrollbarXConfig The horizontal scrollbar config
 * @param props.scrollbarYConfig The vertical scrollbar config
 */
export declare function updateChartScrollbars(props: {
    chart: Chart;
    scrollbarXConfig?: WebChartAxisScrollBar;
    scrollbarYConfig?: WebChartAxisScrollBar;
}): void;
