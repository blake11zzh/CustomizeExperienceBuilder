import type { SerialChart } from "@amcharts/amcharts5";
/**
 * Adds listeners to the chart to handle a selection made by the user.
 * @param chart The chart
 */
export declare function addSelectionOnChart(chart: SerialChart): Promise<void>;
